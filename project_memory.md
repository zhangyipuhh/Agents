# 项目记忆索引

> **读取规则**：先读本索引 → 按关键词定位分片 → 只读目标分片（memory/ 目录）。
> 禁止无目的全量读取任何分片；分片内查找优先 Grep 后按行号区间 Read。
> 历史全文归档：memory/_archive/project_memory_full.md（仅兜底，正常勿读）。

| 分片 | 内容 | 关键词 | 更新时间 |
|---|---|---|---|
| [架构与目录](memory/architecture.md) | 项目概述、技术栈、数据目录约定、项目架构、配置归属隔离、项目文件夹方案、环境变量、**`AuthCookieSettings` 认证 Cookie 配置（HttpOnly Cookie 迁移基础设施，env 前缀 `AUTH_COOKIE_`）**、**`AuthBootstrapSettings`（lifespan 默认管理员创建与历史弱口令迁移，env 前缀 `AUTH_`，fail-loud）**、**等保三级安全编码规范**、**本地 HTTPS 测试 nginx/（mkcert + 8443/8080，与 web/Agent/nginx.conf 同步 + TLS/HSTS 等保三级加固 + HTML 入口禁缓存 `location ~* \.html$`）**、**HTTPS 模块加载踩坑记录（Vite build 的 crossorigin module 在 HTTPS 下需 ACAO 头；nginx 子级 add_header 覆盖父级；Chromium CORS 失败有 network-level negative cache 必须硬刷新）** | 架构、技术栈、目录、路径、环境变量、OwnershipScope、等保三级、安全规范、AuthCookieSettings、HttpOnly Cookie、AUTH_COOKIE_、AuthBootstrapSettings、bootstrap_enabled、AUTH_DEFAULT_ADMIN_PASSWORD、ensure_admin_exists、admin123、123456、history_weak_passwords、nginx、HTTPS、mkcert、8443、8080、HSTS、TLSv1.2、TLSv1.3、HTML 禁缓存、no-cache、.html$、双后端进程、8001、9001、VITE_API_TARGET、checkpointer 回退、MemorySaver、AsyncPostgresSaver、fail-loud、crossorigin、ACAO、CORS、Access-Control-Allow-Origin、模块加载、DevTools Console、硬刷新、Ctrl+Shift+R、network-level negative cache、HTTP 与 HTTPS 差异、172.26.160.50、124.95.179.91、SAN 扩展、rootCA.pem 分发 | 2026-08-16 |
| [安全合规](memory/security-compliance.md) | 等保三级（GB/T 22239-2019）应用层面合规检查清单，覆盖身份鉴别（含「口令复杂度」 ✅ 已落实）、访问控制、安全审计、入侵防范、恶意代码防范、软件容错、数据完整性、数据保密性、数据备份恢复、剩余信息保护、个人信息保护及 HTTPS 快速启动清单 | 等保三级、安全合规、合规检查、GB/T 22239-2019、身份鉴别、口令复杂度、password_policy、validate_password、register、UserDB.create_user、UserDB.update_password、AuthBootstrapSettings、ensure_admin_exists、admin123、123456、history_weak_passwords、刷新令牌、Portal Token | 2026-08-12 |
| [数据库设计](memory/database.md) | 全部表结构、字段、索引、迁移约定、users MFA/TOTP 表与 challenge 表、登录失败锁定字段、JSONB 写入契约、inspection_scripts 表、devops_servers 表、服务器采集双表同事务双写、**`user_login_sessions` 表（2026-08-12 等保三级 §1.5 idle 超时检测：`session_uuid` + `last_active_at` + `revoked_at`/`revoke_reason`；写入 TIMESTAMP 朴素列必须用 `datetime.utcnow()` 禁止 aware datetime）**、**`users.allowed_agents` JSONB 脏数据防御（2026-08-14：`migrate_from_users_allowed_agents` SQL 用 `jsonb_typeof` 守卫把非 array 行归一化为 `'[]'::jsonb`，避免启动迁移 `cannot extract elements from a scalar`；新迁移文件 `2026_08_14_normalize_users_allowed_agents_to_array.sql`）** | 表、字段、迁移、users、MFA、TOTP、mfa_challenges、user_mfa_totp、锁定、jsonb、inspection_scripts、server_inspection_records、user_login_sessions、last_active_at、idle、session_uuid、revoke_reason、allowed_agents 脏数据、jsonb_typeof 守卫、cannot extract elements from a scalar、jsonb_array_elements_text、migrate_from_users_allowed_agents | 2026-08-14 |
| [API 与核心工具](memory/api-routes.md) | API 接口配置、API 路由汇总（路由→文件→权限）、核心工具清单、/api/admin/inspection-scripts 路由（PUT 更新 + 5 字段扫描响应 + DELETE 事务化）、**/api/admin/server-inspection 三端点（GET /latest + GET /records + POST /collect）**、**/login 与 /login-api 同步下发 access_token HttpOnly Cookie + /login-api memory 自动建号调 `password_policy.validate_password`**、**/refresh 轮换 access_token HttpOnly Cookie + /validate 支持 Cookie 兜底** | /api/、路由、端点、require_admin、Core Tools、inspection-scripts、PUT、DELETE、事务、server-inspection、list_latest、collect、OwnershipScope、scope 过滤、save_inspection_result、login、login-api、refresh、validate、access_token、Set-Cookie、HttpOnly Cookie、password_policy、validate_password、auto_register、400 | 2026-08-09 |
| [认证与会话控制](memory/auth.md) | 双 Token 认证、浏览器登录 TOTP 两阶段认证、管理员强制/普通用户可选、challenge 防重放与失败锁定、amr、Portal 子 token、聊天并发控制、HITL、提示词与上下文、MFA 默认 issuer=AIOps（可由 MFA_ISSUER 覆盖）、**`user_mfa_totp.enabled_at` naive TIMESTAMP 列写入约束（修复 offset-aware 误传 bug）**、**登录失败计数与账号锁定（`failed_login_count` / `locked_until`，路由双重保险，修复 DatabasePool.fetchval 缺失导致累计链路静默失效）**、**固定锁定窗口契约（修复 `locked_until` 在活动期间被后续失败请求顺延：PG CASE 与兜底 UPDATE 增加 `locked_until IS NULL OR locked_until <= CURRENT_TIMESTAMP` 守卫，memory 模式增加 `time.time()` 比较，14 个回归用例）**、**Access Token 提取顺序（Bearer 优先 → HttpOnly Cookie 兜底，`request.state.auth_via` 标记来源；Cookie 鉴权写请求中间件强制要求 `X-Requested-With: XMLHttpRequest` 头做 CSRF 二次防线）**、**refresh 成功轮换 access_token HttpOnly Cookie 且 validate 支持 Cookie 兜底**、**Admin 编辑用户不允许改密码（`UserUpdateRequest` 不含 password，密码修改走 `PUT /api/users/{user_id}/password` 且仅本人需 old_password）**、**`口令策略强校验`（`password_policy.validate_password` 同时被路由层 + `UserDB.create_user` / `UserDB.update_password` 持久化层强制调用；登录接口不再执行复杂度校验）**、**`AuthBootstrapSettings`（lifespan 默认管理员创建与历史弱口令迁移，env 前缀 `AUTH_`，启动 `RuntimeError` fail-loud）**、**JWT payload 携带 user_id + 并发会话数量限制（2026-08-11：`generate_token/refresh_token` 显式接受 `user_id`；`refresh_tokens` 表新增 `username` 列；`RefreshTokenDB.count_active_tokens` + `delete_oldest_tokens` 实现踢出最旧会话；`settings.auth.max_concurrent_sessions` 默认 5）**、**会话超时自动退出（2026-08-12 等保三级 §1.5：`settings.auth_idle` + `user_login_sessions` 表 + `idle_timeout_middleware`；`login_session_uuid` Cookie 标识 + `last_active_at` 异步刷新 + idle 30 分钟 401 `idle_timeout` + `check_fail_loud` 数据库失败 fail-loud）** | 认证、MFA、TOTP、双因素、Token、amr、challenge、恢复码、锁定、并发、HITL、提示词、上下文、issuer、AIOps、MFA_ISSUER、enabled_at、naive、datetime、replace(tzinfo=None)、offset-aware、failed_login_count、locked_until、max_attempts、lockout_seconds、record_failed_login、get_login_lock_state、clear_login_lock、DatabasePool.fetchval、CTE、fetchrow、双重保险、logger.exception、except Exception: pass、固定锁定窗口、固定窗口、活动锁定、不顺延、CURRENT_TIMESTAMP、Bearer、HttpOnly Cookie、access_token、auth_via、CSRF、X-Requested-With、XMLHttpRequest、二次防线、refresh、validate、access_token Cookie、X-Requested-With CSRF、HttpOnly Cookie 鉴权、AuthCookieSettings、admin 不改密码、UserUpdateRequest 不含 password、old_password 仅本人、password_policy、validate_password、special_char_whitelist、MIN_LENGTH_8、complexity_check、login_complexity_off、JWTAuth、bootstrap_username、bootstrap_password、verify_credentials、AuthBootstrapSettings、bootstrap_enabled、AUTH_DEFAULT_ADMIN_PASSWORD、ensure_admin_exists、history_weak_passwords、admin123、123456、refresh_token_revoke、portal_token_revoke、JWT user_id payload、generate_token user_id、并发会话、max_concurrent_sessions、踢出最旧、delete_oldest_tokens、count_active_tokens、refresh_tokens.username 列、ALTER COLUMN IF NOT EXISTS、ROW_NUMBER OVER、AuthBootstrapSettings.max_concurrent_sessions、AUTH_MAX_CONCURRENT_SESSIONS、idle、idle_timeout、auth_idle、timeout_seconds、check_enabled、check_exempt_paths、check_fail_loud、AUTH_IDLE_TIMEOUT_SECONDS、user_login_sessions、session_uuid、last_active_at、revoke_reason、UserLoginSessionService、idle_timeout_middleware、fire-and-forget、asyncio.create_task、503 idle_check_unavailable、等保三级 §1.5、无操作自动退出、ops-detect 临时会话自动供给（session_auth_middleware 扩域 + get_user_sessions 过滤前缀） | 2026-08-17 |
| [Agent 与 Skill 体系](memory/agents-skills.md) | Agent 统一构造入口、Skill 系统、AGENTS.md 加载器、配置加载服务、工具注册中心、缓存、工具绑定双轨制、记忆存储、scripts/ | Agent、Skill、bootstrap、工具注册、缓存、记忆存储 | 2026-07-26 |
| [MCP 体系](memory/mcp.md) | MCP 配置 CRUD、MCP/Tool/Skill Admin Router、统一 Agent Router、MCPToolsRegistry | MCP、服务器、工具注册、Admin Router | 2026-07-26 |
| [前端架构](memory/frontend.md) | web/Agent 前端架构、LoginView TOTP 两阶段登录（含 mfa_enroll 阶段恢复码"我已抄写并继续"按钮，2026-08-08 修复一闪而过 bug）、UserSettingsDialog MFA 管理、UserSettingsDialog 系列、斜杠命令注册表、触发器注册表（# 行内服务器 Chip）、TriggerPanel、MessageBubble mention 统一渲染、contenteditable 编辑器 DOM 工具、InputBox 会话切换清理本地态、TaskSchedulerManager context_overrides 参数化编辑器（2026-07-29）、巡检脚本按需两段式加载 + 脚本库扫描面板（2026-08-03）、巡检脚本库独立 Tab + 节点列表（含行尾删除按钮 + 二次 confirm + emit select null 反向同步 + 扫描成功即时刷新列表）+ 编辑保存面板（2026-08-04）、服务器扫描入库 Tab 切回时强制刷新 inspectionScripts 下拉（剥离 hasLoaded 短路，devopsServers 仍短路，2026-08-05）、运维控制台独立入口 ops-console.html + 7 个 Ops* 组件（2026-08-05）、Sidebar 运维控制台 / 知识库按钮改为新 Tab 打开（去掉 features 参数，行为等同 target="_blank"，2026-08-05）、OpsConsoleApp 去 mock 化（GET /api/admin/server-inspection/latest 替换 mockData.servers）+ OpsServerIcon unknown 灰色 LED 三态 + OpsDetailWindow 智能检测接 collect 输出真实判定 + 3 个 api 函数（2026-08-05）、前端 XSS 加固（2026-08-07）: src/utils/sanitize-marked.js + DOMPurify + MessageBubble/FilePreview marked → safeMarkdown + InputBox sanitizeEditorHtml 升级属性白名单 + handlePaste 现代 Range API + PortalApp iframe safeIframeUrl 协议过滤（**未**加 sandbox / referrerpolicy / loading=lazy 保守起见）+ nginx.conf 加 CSP / X-Frame-Options / X-Content-Type-Options / Referrer-Policy + jsdom devDep、**ProfileInputBox.vue 预存 bug 修复（2026-08-07）: 补 onMounted import,导致规则库 iframe 显示空白**、**运维控制台 ops-console.html 等保三级改造（2026-08-08）: 删除独立 HTML 入口改为 App.vue::currentPage === 'ops-console' 内嵌 OpsConsolePage + 复用主应用 HttpOnly Cookie + fetchWithAuth + X-Requested-With CSRF 头鉴权链路 + ops-console.css 改 .ops-console-root 作用域前缀 + ensureOpsConsoleStyles() 按需引入 + Sidebar handleMenuClick 改 emit('page-change', 'ops-console')**、**项目采用 vue-router 4（2026-08-XX）: 路由表含 /、/knowledge、/ops-console 三个一级路由 + not-found 兜底 + main.js 接 .use(router) + App.vue 改 router-view + views/{AgentWorkspace,KnowledgeWorkspace,OpsConsoleWorkspace} + chatWorkspace provide/inject 链路 + Sidebar 改 router.push + nginx.conf 加 /ops-console location + App.vue 移除 currentPage/handlePageChange + utils/ops-console-styles.js 抽出 ensureOpsConsoleStyles 单例守卫 + KnowledgePage 移除 page-change event + router meta 已建 menuAcl/requiredRole/ownershipScope/auditEvent/auditFields/redactFields/csrf/sessionTimeoutSec/sensitiveOperation/logoutOnLeave 9 个等保扩展字段空 schema**、**chatWorkspace 必须用 reactive(...) 包裹（2026-08-08）: 防止 vue 3.4+ template compiler 不解包嵌套 ref 导致 FilePreviewModal 等弹窗始终显示 + content undefined;Sidebar useRoute() 加 route undefined 保护防单测 mount / PortalApp iframe 等无 router 场景崩溃**、**运维控制台「完整独立界面 + 关闭回主会话」（2026-08-09）: App.vue 加 useRoute()+isOpsConsoleRoute 走 v-else-if 分支独占 viewport 不挂 Sidebar/ProjectDialog/SubAgentDrawer（useRoute happy-dom 兜底 window.location.pathname）;OpsMenuBar 顶部 mac 红/黄/绿点（红点 emit('exit')，键盘可达 + aria-label）→ OpsConsoleApp defineEmits(['exit'])+ 透传 → OpsConsoleWorkspace.handleExit 优先 window.close()（window.opener / history.length=1 判定）失败/无 opener 降级 router.push('/');ops-console.css 加 .ops-console-root .menubar .menubar-traffic 样式;3 个新 spec 全绿**、**OpsConsoleApp logFolders 去前端 mock（2026-08-12）: 移除 ``src/data/ops-console/mockData.js``（被 Vite 打生产 bundle 污染 Docker 镜像），mock 数据迁入 ``src/components/ops-console/__tests__/fixtures/opsConsoleMockData.js``，OpsConsoleApp.vue 改为本地 ``ref([])`` 等待后端 ``GET /api/admin/log-folders`` 落地;新增契约测试 ``logFoldersDataSource.spec.js``（P0 fixture 可导入 + 数据结构完整 + 源码不含 src/data 路径字符串 + 不含 ``import { logFolders }`` + 仍声明本地 ref）防止后续误回退**、**运维控制台 GNOME / Ubuntu 风格化（2026-08-13，详见 memory/frontend.md 「运维控制台 GNOME / Ubuntu 风格化」章节）：macOS 风格（圆角 + 毛玻璃 + 三色交通灯 + 底部 Dock 弹跳）→ GNOME 风格（窗口直角 + 标题栏右侧 max/close 两按钮原生 button + SVG + 顶部菜单栏原生 ✕ Close 替代 mac 红点 + 底部 taskbar 政务蓝实色整宽 36px 替代 mac Dock + ops-console.css 全栈重写 + OpsDockBar 模板类名 dock-* → taskbar-* + LED 边框政务蓝化 + OpsMenuBar/4 窗口模板调整）；本轮不实现 minimize（涉及最小化栈与状态保留，单独 PR 落地）；窗口事件契约 close/max/emit exit 完全不变，4 个 spec 共 17 用例全绿**、**运维控制台菜单栏重构（2026-08-14，用户需求）: 顶部菜单栏三段式布局（左应用名 + 中 服务器管理/日志管理 两按钮 flex:1 center 居中 + 右时间 + ✕ Close）；删除 文件/编辑/视图/帮助 中文全局菜单占位；删除 OpsDockBar 组件（彻底删除）+ .taskbar-* CSS 全套删除 + .win.maximized 高度回 calc(100vh - 28px)；彻底删除 detectAll / detailRef / nextTick 一键智能检测链路（死代码清理）；OpsMenuBar 新增 emit('open', 'servers'|'logs') → OpsConsoleApp 透传给 openWin；零业务逻辑改动（4 窗口组件未触碰）；4 个 spec 共 21 用例全绿（17 → 21，新增 4 个）**、**密码框显示/隐藏切换 PasswordInput.vue（2026-08-14，用户需求）: 新增共享组件封装眼睛 SVG 切换（heroicons 风格 24x24 stroke）+ aria-label/aria-pressed；两种模式（默认切 type ↔ usePasswordMask=true 切 .password-mask class 避免改 type 触发密码管理器重置）；登录 #login-password + 注册 #register-password + #register-confirm-password + UserSettingsDialog 6 处（修改密码 3 处含 usePasswordMask / MFA 当前密码 2 处 / admin 创建编辑用户 1 处）共 9 个密码框接入；零运行时依赖；4 个 spec 共 ~23 用例全绿**、**运维控制台 OpsServerWindow 卡片增强两轮（2026-08-16）: 第一轮 卡片头最后检测时间（绝对时间 YYYY-MM-DD HH:MM，显示「最新检测时间:」前缀，无快照 `最新检测时间:-`）+ 指标行单行追加「服务器负载」项（仅 linux 显示）+ 后端 _derive_metrics 扩为 {cpu,mem,disk,load}（linux 取 parsed_values.load_1m / windows None 零迁移）+ 前端 OpsConsoleApp mapSnapshotToServer 补 serverType/load+ 新增具名导出函数 formatCollectedAt/isLinuxType/loadColor（负载阈值 ≥4 红独立于 80% 阈值，对齐 inspection_scripts.yaml load_1m warn=4.0）;第二轮 存储行基于后端 field_results 智能选异常盘（pickAnomalyDisks 具名函数按 mount 聚合 warn/crit 项，盘符 + 异常概要统一标红 #ff453a，多盘符 flex-wrap 换行;全 pass → '-';fieldResults 空 → 兜底既有 pickDisplayDisk）+ linux 卡片负载 label 「负载」→「服务器负载」+ 后端 _row_to_view/_merge_user_view 视图 dict 透出 field_results（数据库 server_latest_snapshot.field_results JSONB 已落库只缺透出）+ 前端 mapSnapshotToServer 补 fieldResults + disks[] 补 mount/ioUtilPct/ioAwaitMs/diskType + ops-console.css 追加 .srv-metric-disk--problem/.srv-metric-disk-name/.srv-metric-anomaly/.srv-metric-storage 样式 + 后端 3+2=5 / 前端 12+10=22 个新用例全绿（57 → 62 / 35 → 45）**、**运维控制台 OpsDetailWindow 详情页改造（2026-08-16，用户需求）: 移除「智能检测」按钮 + detect-panel + runDetect 整段逻辑（详情页只读）；头部 sub 由 `ip · os` 改为「最新检测时间:YYYY-MM-DD HH:MM」绝对时间（复用 OpsServerWindow.formatCollectedAt）；顶部 3 个圆角指标卡（含 .bar 进度条）→ 删除，改为与外层卡片同款长方形 4 联指标条（.detail-metric-bar：CPU 使用率 / 内存占用 / 存储使用 / 服务器负载[仅 linux]），复用 metricColor/loadColor/isLinuxType 阈值口径；kv 表格精简：移除「内存总量/存储总量/网络流入」3 项，保留「操作系统/CPU 型号/运行时长」3 项；磁盘展示由单列行（盘符 + 已用% + 共 + 进度条）→ 多列网格（.disk-grid，每列盘符 + 三指标：使用率/排队ms/IO利用率），完全去除磁盘进度条样式；详情窗口宽度 500 → 460（用户要求「四联的框需要缩小」）；ops-console.css 删除 .metrics/.metric/.bar/.disk-row/.gov-btn/.detect-panel/.cursor 等死样式；新增 OpsDetailWindow.redesign.spec.js 19 个用例全绿；5 个 ops-console spec 文件共 81 个用例全绿**、**运维控制台 OpsDetailWindow 磁盘物理盘纵向分组（2026-08-16，用户需求，Linux+Windows 双平台）: 磁盘展示由「多列网格」改为「按物理盘纵向分组」；磁盘头（`.disk-group-head`）显示 LED +「磁盘 N」+ `#N` + hostDisk 设备名 + 「排队 / IO 利用率」两指标（同组多记录取 max）；分区卡（`.disk-pcard`）只显示「使用率」；整盘 IO 记录（`partition=""`）不渲染为分区卡，只在磁盘头聚合 IO 数据；数据契约：`disks[].hostDisk` + `disks[].diskIndex` + `disks[].partition`（Linux `lsblk -o NAME,PKNAME` 推得；Windows `Win32_PerfFormattedData_PerfDisk_PhysicalDisk` 实例名解析）；`OpsDetailWindow.vue::groupDisksByPhysicalDisk` 模块级纯函数（分组键优先级 `disk:${diskIndex}` > `host:${hostDisk}` > `legacy:${mount|name}` + 兼容历史 Windows `0 C: D:[SSD]` mount 解析 + Intl.Collator numeric 自然排序）；`OpsDetailWindow.redesign.spec.js` 重写为 27 用例（含 Linux sda 同盘多分区 + Windows C:/D: 归 PHYSICALDRIVE0 + IO 整盘记录不渲染分区卡反向用例）；ops-console.css 新增 `.disk-groups` flex column + `.disk-group` 圆角边框 + `.disk-group-head` flex + `.disk-group-partitions` flex-wrap + `.disk-pcard` flex:1 1 112px；6 个 ops-console spec 文件共 98 用例全绿（88 → 98）**、**运维控制台 OpsServerWindow 卡片头操作按钮 + OpsInspectionLogWindow 采集记录窗口（2026-08-17，用户需求）: 卡片头 DOM 顺序 [LED] [服务器名] [最新检测时间] [日志] [智能检测]，时间贴近名称 + 同行追加两个 22×22 图标按钮（日志可用 emit('open-log', srv) / 智能检测 disabled 占位 .srv-card-btn--disabled 灰显 + title 暂未开放 + @click.stop 阻断冒泡）；日志按钮触发 OpsConsoleApp.openInspectionLog(srv) 拉取 GET /api/admin/server-inspection/records?server_id=X&limit=100 后挂载新窗口；新窗口 OpsInspectionLogWindow.vue（760×460，左 280px .inslog-list 列表 + 右自适应 .inslog-detail 详情复用 OpsDetailWindow 同样式）：左栏每行展示「formatCollectedAt 时间 + inslog-badge 状态徽章（pass/warn/crit/skipped/unassessed 五态色块） + 耗时 formatDuration + exit_code + 成功/失败 + error_message 红字摘要」+ 默认首条 active + 点击切换 + 空态/loading 态；右栏通过 mapRecordToServer(record, baseServer) 纯函数把后端 parsed_values/field_results 归一化为 ServerItem 形状 inline 渲染（避免嵌套窗口外壳造成双重标题栏）；OpsDetailWindow 双 script 块改造（纯函数从 <script setup> 搬到 <script> 加 export + 新增 statusLabel/formatDuration）；OpsConsoleApp 状态机新增 wins.inspectionLog/inspectionLogServer/inspectionLogRecords/inspectionLogLoading 4 项 + openInspectionLog/closeInspectionLog/onOpenDetect 3 个函数；零后端改动（records 接口 + 表结构 + ACL 全部复用现有）；零菜单新增（不进 MENU_CATALOG，复用 task-scheduler.server-management ACL）；2 个 spec 文件（OpsServerWindow.card.spec.js +5 用例、OpsInspectionLogWindow.spec.js 新增 13 用例）全绿** | 前端、Vue、组件、UserSettingsDialog、斜杠命令、triggerRegistry、TriggerPanel、inputEditor、会话切换、context_overrides、reference_server、inspection-scripts、巡检脚本、inspection-script-library、InspectionScriptLibraryPanel、InspectionScriptEditorPanel、删除按钮、行尾 delete、refreshToken、switchTab、hasLoaded、Tab 刷新、运维控制台、ops-console、OpsConsoleApp、政务蓝、多窗口、Sidebar handleMenuClick、ops-console.html、新 Tab 节点、target=_blank、_blank、noopener、OpsServerIcon unknown、去 mock、mapSnapshotToServer、collectServerInspection、runDetect、loadLatest、XSS、sanitize-marked、safeMarkdown、DOMPurify、marked、async false、afterSanitizeAttributes、safeIframeUrl、CSP、Content-Security-Policy、X-Frame-Options、X-Content-Type-Options、Referrer-Policy、ProfileInputBox、onMounted、知识库空白、welcome-section、recovery_codes、一闪而过、pendingLoginAuth、mfa-recovery-ack-btn、recovery block、ops-console 等保三级、内嵌子页面、currentPage='ops-console'、ensureOpsConsoleStyles、按需引入 CSS、ops-console-root 作用域、政务蓝样式污染、vue-router、vue-router 4、vue-router 接入、router.push、router-view、createWebHistory、beforeEach、requiresAuth、pageKey、hasLocalAuthToken、redirect 参数、Workspace、AgentWorkspace、KnowledgeWorkspace、OpsConsoleWorkspace、chatWorkspace、provide、inject、router meta、menuAcl、requiredRole、ownershipScope、auditEvent、auditFields、redactFields、csrf、sessionTimeoutSec、sensitiveOperation、logoutOnLeave、meta 等保扩展、ops-console-styles、ops-console.css 单例、nginx /ops-console location、SPA fallback、OpsConsoleWorkspace onMounted、KnowledgePage page-change 移除、App.vue 瘦身、scoped CSS 不穿透、styles/layout.css、全局 layout、AgentWorkspace 坍缩修复、chatWorkspace reactive 包裹、嵌套 ref 解包、FilePreviewModal 自动弹出 bug、useRoute() 保护、route undefined 兜底、requiresAuthGuard、整页跳 /login、禁止守卫应用内 return path /login、无限重定向循环、白屏、运维独立界面、App.vue 路由级 layout、isOpsConsoleRoute、OpsConsoleWorkspace exit、OpsMenuBar 三色点、menubar-traffic、政务蓝退出、window.close 优先、opener 判定、history.length=1、router.push('/') 降级、logFolders 去 mock、mockData 污染 Docker、src/data 移除、__tests__/fixtures、opsConsoleMockData、契约测试、logFoldersDataSource.spec、生产 bundle 污染、运维控制台 GNOME / Ubuntu 风格化、运维控制台菜单栏重构、menubar-left、menubar-center、menubar-right、menubar-nav-btn、三段式布局、OpsDockBar 删除、detectAll 删除、detailRef 删除、nextTick 删除、菜单占位移除、文件 编辑 视图 帮助 移除、PasswordInput、密码显示切换、眼睛图标、usePasswordMask、aria-pressed、password-mask、text-security、inline SVG、heroicons、运维控制台 OpsServerWindow 卡片增强、最后检测时间、YYYY-MM-DD HH:MM、绝对时间、srv-card-time、卡片头右贴、formatCollectedAt、isLinuxType、loadColor、LOAD_WARN=4、Linux 1 分钟负载、load_1m、parsed_values.load_1m、v-if linux、_derive_metrics load 字段、metrics.load、windows None、白名单 platform、运维控制台 OpsServerWindow 卡片磁盘异常指标、磁盘异常智能展示、pickAnomalyDisks、formatAnomalyItem、field_results 透出、_row_to_view field_results、_merge_user_view field_results、anomaliesById、srv-metric-disk--problem、srv-metric-disk-name、srv-metric-anomaly、srv-metric-storage、flex-wrap 换行、阈值复算走数据库、磁盘 IO 利用率、io_util_pct、磁盘 IO 平均等待、io_await_ms、disk_used_pct、磁盘使用率、异常盘符、message 解析 mount、「磁盘 {mount}」、InspectionFieldResult.vars()、disks[] 兜底 mount、anomaliesOf、formatAnomalyItem、formatAnomalyItem 单位 %/ms、服务器负载 label、「最新检测时间:」前缀、运维控制台 OpsDetailWindow 详情页改造、智能检测按钮删除、detect-panel 删除、runDetect 删除、collectServerInspection 移除、4 联指标条、detail-metric-bar、长方形指标卡、CPU 使用率 内存占用 存储使用 服务器负载、kv 精简、内存总量 存储总量 网络流入 删除、磁盘多列网格、disk-grid、disk-cell、dc-head、dc-name、dc-metrics、dc-m、dc-m-label、dc-m-value、disk-empty、使用率 排队 IO 利用率、磁盘进度条 删除、win-detail 460px、缩窄窗口、放大的感觉、运维控制台 OpsDetailWindow 磁盘物理盘纵向分组、物理盘分组、groupDisksByPhysicalDisk、disk-groups、disk-group、disk-group-head、disk-group-partitions、disk-pcard、dgh-name、dgh-index、dgh-device、dgh-metrics、dcp-header、dcp-name、host_disk、hostDisk、disk_index、diskIndex、partition、lsblk、PKNAME、Win32_DiskDrive、Win32_PerfFormattedData_PerfDisk_PhysicalDisk、PHYSICALDRIVE0、Intl.Collator numeric、分区卡、分区、disk-section、分区记录、整盘 IO 记录、IO 记录不渲染分区卡、partition 非空、Linux sda、Windows PHYSICALDRIVE、运维控制台 OpsServerWindow 卡片头操作按钮、卡片头 DOM 顺序、srv-card-btn、22×22 图标按钮、@click.stop 阻断冒泡、open-log emit、open-detect 占位 disabled、OpsInspectionLogWindow、采集记录窗口、左 280 列表、右自适应详情、inslog-list、inslog-item、inslog-badge 状态徽章、formatDuration、inslog-err、mapRecordToServer 纯函数、OpsDetailWindow 双 script 块、statusLabel、formatDuration 友好展示、wins.inspectionLog、inspectionLogServer、inspectionLogRecords、inspectionLogLoading、openInspectionLog 异步拉取、零后端改动、零菜单新增、OpsDetectChatWindow 智能检测流式聊天窗口（2026-08-17 新增）：OpsServerWindow 卡片头「智能检测」按钮 emit('open-detect', srv) → OpsConsoleApp.onOpenDetect 打开独立浮动窗口、onMounted 自动调 chatStream 一次（合成 ops-detect:{server_id}:{ts} session_id 不落 sessions 表不污染主侧边栏、DETECT_QUESTION 固定两段式问题文本、agent_name='project'、extras={referenced_servers:[{name: businessName, server_type}]} 注入 server.business_name）、SSE 流式渲染 safeMarkdown（DOMPurify XSS 加固）、onBeforeUnmount 调 triggerAbort + reader.cancel 优雅中止、mapSnapshotToServer 透传 businessName 字段供 query_inspection_records business_name 精确反查契约对齐、HTTP 403 agent ACL 缺失/429 排队/网络错误转错误横幅、加载占位 detect-empty 心跳（v-if / v-else-if / v-else 三段互斥：错误横幅 → 「正在分析服务器巡检数据…」政务蓝 8px 圆点 + 1.2s 透明度心跳 → 流式回答卡片，覆盖「流式发起后首段 SSE 到达前」整段加载期）、零后端改动、零菜单新增 | 2026-08-17 |
| [统一审计日志（2026-07-29）](memory/architecture.md#统一审计日志2026-07-29) | 统一日志 `LogService` 唯一入口；SSH/devops 事件；可信身份 + 可信 IP；管理员查询信封 | LogService、审计日志、SSH、devops、可信身份、可信 IP、log_ip、查询信封 | 2026-07-30 |
| [菜单权限与用户配置](memory/menu-acl.md) | 用户菜单权限管理（MENU_CATALOG/ACL，含 `task-scheduler.inspection-script-library` 巡检脚本库菜单）、用户服务器配置管理、**/api/admin/server-inspection 三端点 ACL 矩阵（复用 `task-scheduler.server-management`，2026-08-05）** | 菜单、权限、ACL、allowed_agents、服务器配置、inspection-script-library、server-inspection、server-management | 2026-08-05 |
| [DevOps 与沙箱](memory/devops-sandbox.md) | SSH 远程服务器管理、巡检脚本库（inspection_scripts / InspectionScriptService，含 update_script_detail + delete_script 单事务 pool.acquire + conn.transaction + 编辑优先扫描 + ON DELETE SET NULL）、lifespan 强依赖顺序、`get_connection_config` 返回 14 键（脚本库元数据 4 键透传到 `ServerOpsItem`）、沙箱 Agent 架构、SubAgent 事件协议、SSHTools.execute_command 第三方加密分支（RSA-OAEP + AES-256-GCM，2026-08-03；**2026-08-05 改 async def 消除 LangGraph in-flight loop RuntimeError**）、**巡检 IO 指标 + SSD/HDD 介质差异化阈值（2026-08-15：`InspectionFieldRule` ssd_warn/ssd_crit + Linux `/proc/diskstats` 双采样 + Windows `Win32_PerfFormattedData_PerfDisk_PhysicalDisk` + ops_report docx 磁盘介质清单表）**、**巡检脚本当前 inspection_fields 契约（2026-08-16：Linux 9 条 含 io_util_pct/io_await_ms + Win 8 条多 cpu_used_pct；不含 os/cpu_model/uptime_hours 展示型字段；ops_report `_server_meta_rows` 元信息表固定 5 行 `业务名/服务器 IP/SSH 退出码/耗时/巡检状态`，历史 JSONB 残留键吞值不渲染）**、**巡检 OS 关键指标告警归类与物理盘列（2026-08-16：cpu_iowait_pct/swap_used_pct/inode_used_pct 3 字段在 metric 加「（OS 指标）」后缀 + 报告「OS 关键指标」小节按 warn/crit 触发条件渲染 + `_format_host_disk` 按 host_disk[disk_index]/partition 优先级渲染「物理盘」列 + `_server_disk_inventory_rows` 扩为 6 列 + 解析/服务层零改动 + test_ops_report.py 23 → 29 全绿）**、**磁盘介质清单表按 host_disk 分组渲染（2026-08-16：元素含 host_disk 时按其分组；每组 1 行 sub-header `[host_disk, 介质汇总, -, -, -, -]`（介质汇总：组内 disk_type 去重后 `+` 拼接）+ N 行 partition/IO 数据；组内按 `(disk_index, mount)` 排序；组间按 `(min disk_index, host_disk)` 排序；全缺 host_disk 退化为平铺（兼容 2026-08-15 旧契约 / 历史快照）；部分缺 host_disk 归入 `_orphan_` 虚拟组排最后；test_ops_report.py 新增 4 用例：`groups_same_host_disk` / `groups_multiple_host_disks` / `orphan_group_for_missing_host_disk` / `keeps_flat_when_all_missing_host_disk`；ops_report 29 → 33 全绿）**、**query_inspection_records latest_only=False start/end 收紧为可选（2026-08-17 ops-detect 智能检测窗口死循环根因修复：双缺透传 None 返回最近 limit 条 + 单侧缺失同理；payload/审计 metadata 加 time_range_defaulted 标记；invalid_time 仍报；test_inspection_query_tools.py 16 → 19 全绿含 3 新用例）** | DevOps、SSH、沙箱、SubAgent、事件协议、inspection_scripts、InspectionScriptService、update_script_detail、delete_script、事务、asyncpg、pool.acquire、conn.transaction、编辑优先、ON DELETE SET NULL、get_connection_config、14 键、ServerOpsItem、脚本库元数据、第三方执行器、第三方加密、use_third_party_executor、third_party_endpoint_name、ssh_config、ThirdPartyEndpointRegistry、execute接口说明、async def、asyncio.to_thread、in-flight loop、RuntimeError、endpoints_json、AliasChoices、THIRD_PARTY_EXECUTOR_ENDPOINTS、_ENV_FILE_PATH、env_file、CWD、_read_env_file_endpoints_fallback、环境变量污染、兜底、IO 指标、io_util_pct、io_await_ms、/proc/diskstats、/sys/block、rotational、Win32_PerfFormattedData_PerfDisk_PhysicalDisk、MSFT_PhysicalDisk、MediaType、disk_type、SSD/HDD 介质、ssd_warn、ssd_crit、InspectionFieldRule 8 字段、_expand_disks_array 介质匹配、_server_disk_inventory_rows、磁盘介质清单表、元信息表 5 行、_server_meta_rows 简化、os 移除、cpu_model 移除、uptime_hours 移除、展示型字段、inspection_fields 当前契约、历史 JSONB 兼容、吞值不渲染、_OS_METRIC_KEYS、_format_host_disk、物理盘列、host_disk、disk_index、partition、PHYSICALDRIVE、6 列表、column_widths、磁盘清单表分组、sub-header、介质汇总、_orphan_ 虚拟组、平铺退化、host_disk 分组、组内排序、组间排序 | 2026-08-17 |
| [其他子系统](memory/misc.md) | 邮件系统、飞书工具、CI 测试 | 邮件、飞书、CI、GitHub Actions | 2026-07-31 |

## API 路由速查表

> 注册入口统一在 `app/main.py::register_routers`；端点级明细见 [api-routes 分片](memory/api-routes.md)。
> 「登录态」= 依赖全局 auth_middleware，无额外角色校验；「router 级」= `APIRouter(dependencies=[...])` 统一守护。

| 前缀 | 路由文件 | 权限 |
|---|---|---|
| /api/auth | app/shared/routers/auth_router.py | login/register/captcha 公开，其余登录态 |
| /api/users | app/shared/routers/user_router.py | 管理端点 require_admin；profile/username 登录用户限本人 |
| /api/session | app/shared/routers/session_router.py | 登录态；/admin/* 逐端点 require_admin |
| /api/files | app/shared/routers/file_router.py | 登录态 |
| /api/project | app/shared/routers/project_router.py | 登录态 |
| /api/core | app/core/router/file_upload_router.py | 登录态 |
| /api/core/download | app/core/router/file_download_router.py | 登录态 |
| /api/contract | app/features/contract_host_agent/router/contract_router.py | 登录态 + chat_concurrency_dependency 并发控制 |
| /api/map | app/routers/knowledge_router.py | 登录态 |
| /api/ai-coding-check | app/features/AI_Coding_Check_agent/router/ai_coding_check_router.py | 登录态 |
| /api/agent | app/routers/agent_router.py | 登录态；内部按 request.state.role 过滤可见 agent（allowed_agents） |
| /api/admin/agents | app/routers/agent_admin_router.py | require_admin（router 级） |
| /api/admin/mcp | app/routers/mcp_admin_router.py | 登录态（详见 mcp 分片） |
| /api/admin/tools | app/routers/tool_admin_router.py | require_admin（router 级） |
| /api/admin/skills | app/routers/skill_admin_router.py | require_admin（router 级） |
| /api/admin/scripts | app/routers/script_admin_router.py | require_admin（router 级） |
| /api/admin/task-schedules | app/routers/task_scheduler_router.py | 逐端点 require_admin_or_menu_acl('task-scheduler.scheduled') |
| /api/admin/email | app/routers/email_admin_router.py | 逐端点 require_admin_or_menu_acl('task-scheduler.email-settings.*') |
| /api/admin/api-configs | app/routers/api_config_router.py | 逐端点 require_admin_or_menu_acl('task-scheduler.api-config') |
| /api/admin/devops-servers | app/routers/devops_server_admin_router.py | 列表端点 require_admin_or_menu_acl('task-scheduler.server-management')，其余 require_admin；详情端点 2026-08-03 改造为返回 inspection_script_id / inspection_script_name / inspection_script_display_name 元数据（脚本原文改走 /api/admin/inspection-scripts/{id}） |
| /api/admin/inspection-scripts | app/routers/inspection_script_admin_router.py | 列表 require_admin_or_menu_acl('task-scheduler.server-management')；/scan / PUT / DELETE /{script_id} 仅 admin（PUT/DELETE 后续可基于 ACL 扩展） |
| /api/admin/server-inspection | app/routers/server_inspection_router.py | 三端点全部 require_admin_or_menu_acl('task-scheduler.server-management') + OwnershipScope 数据层过滤（admin 透传全量 devops_servers，普通用户按 user_server_nodes 可见集去重）；POST /collect 额外逐 server_id 校验归属，越权 403 / 不存在 404；手动采集内部合成 ScriptContext（schedule_id=0/run_id=0/trigger_type='manual'）复用 run_server_ops + save_inspection_result |
| /api/admin/user-servers | app/routers/user_server_router.py | 逐端点 require_admin_or_menu_acl('task-scheduler.server-management') |
| /api/admin/permissions | app/routers/menu_permission_router.py | require_admin（router 级） |
| /api/admin/permissions/agents | app/routers/agent_permission_router.py | require_admin（router 级） |

## 前端组件速查表

> 根目录均为 `web/Agent/src/`；「展示层」= 数据由父组件注入，不直接调 API。
> API 函数统一封装在 `src/utils/api.js`（`fetchWithAuth` 自动注入 Bearer + X-Session-ID + 401 刷新重试）。

| 分组 | 组件 | 数据来源 API |
|---|---|---|
| 入口 | App.vue（index.html） | /api/session/*、/api/agent/chat（SSE）、/api/auth/* |
| 入口 | views/AgentWorkspace.vue（/） | 注入自 App.vue 的 chatWorkspace（ChatArea/InputBox/QueueStatusBanner/HumanApprovalBox/SessionFileDrawer/FilePreviewModal/DislikeDialog） |
| 入口 | views/KnowledgeWorkspace.vue（/knowledge） | KnowledgePage 透传 |
| 入口 | views/OpsConsoleWorkspace.vue（/ops-console） | OpsConsoleApp + onMounted 调 ensureOpsConsoleStyles |
| 入口 | KnowledgeApp.vue（knowledge.html） | /api/map/knowledge-chat（SSE）、/api/map/knowledge/files |
| 入口 | PortalApp.vue（portal.html） | public/app-config.json（运行时配置）、/api/auth/* |
| 入口 | views/LoginView.vue / RegisterView.vue（login.html） | /api/auth/captcha、login、register |
| 聊天 | ChatArea.vue / MessageBubble.vue / TopBar.vue | 展示层（App.vue 注入） |
| 聊天 | TriggerPanel.vue | 通用触发器面板（搜索 + 平铺 + 键盘导航；由 InputBox 注入 items/searchKeys） |
| 聊天 | InputBox.vue | /api/agent/list、/api/core/upload*、/api/core/upload-config、DELETE /api/core/attachments、/api/admin/user-servers/tree（# 触发器数据源） |
| 聊天 | KnowledgeChat.vue / ProfileInputBox.vue | /api/map/knowledge-chat（SSE）、/api/core/upload* |
| 聊天 | HumanApprovalBox.vue | HITL resume（经 chatStream 透传） |
| 聊天 | QueueStatusBanner.vue | SSE queue 事件 / HTTP 429 |
| 聊天 | SubAgentCard / SubAgentDrawer / ToolCallCard / SkillTags / SubAgentSuggestionStrip | 展示层（SSE 事件流解析自 sseParser.js） |
| 聊天 | DislikeDialog.vue | POST /api/agent/message-feedback |
| 会话/项目 | Sidebar.vue | /api/session/list、delete、title、export；/api/project/* |
| 会话/项目 | ProjectDialog.vue / ProjectDropdown.vue | /api/project/create、/list |
| 文件 | SessionFileDrawer / FolderTree / FilePreviewModal / FilePreview | /api/session/{id}/files/tree、preview、download |
| 文件 | FileManagerModal.vue / KnowledgePage.vue（旧版） | /api/map/knowledge/files、file-preview |
| 设置 | UserSettingsDialog.vue（8 Tab 容器） | /api/users/*、/api/session/admin/* |
| 设置 | AgentManager.vue（含 SectionEditor 子组件） | /api/admin/agents/*、/api/admin/tools、/api/admin/mcp/* |
| 设置 | McpServerManager.vue | /api/admin/mcp/servers/* |
| 设置 | ToolManager.vue | /api/admin/tools/* |
| 设置 | SkillManager.vue | /api/admin/skills/* |
| 设置 | TaskSchedulerManager.vue | /api/admin/task-schedules/*、/api/admin/devops-servers/*、/api/admin/user-servers/tree、/api/admin/scripts/*、/api/admin/email/policies、/api/admin/api-configs/tree |
| 设置 | EmailSettingsManager.vue | /api/admin/email/* |
| 设置 | ApiConfigManager.vue | /api/admin/api-configs/* |
| 设置 | MenuPermissionManager.vue | /api/admin/permissions/menu-catalog、/users/{id}/grants |
| 设置 | AgentAccessManager.vue | /api/admin/permissions/agents/catalog、/users/{id}/grants |
| 设置 | UserServerManager.vue / ImportServerDialog.vue | /api/admin/user-servers/*、/import；/api/admin/devops-servers（导入源列表） |
| 其他 | HelloWorld.vue | 脚手架示例，无 API |

## 写入规则

- 修改某主题 → 只 Edit 对应分片，并更新本索引对应行的「更新时间」。
- 新增章节 → 写入对应分片；若无合适分片，在 memory/ 新建分片并在本索引登记一行。
- 只记录最终/当前状态，变更历史查 git log。

## 变更日志
- **2026-08-16**：scripts 脚本系统接入第三方 SSH：`ops_inspection_sweep.params_schema` 加 `use_third_party_executor` (bool) + `third_party_endpoint_name` (str) 两个键；`run_server_ops` / `_run_one` 加同款 keyword 参数；`execute_third_party_script` 同步薄壳复用 `third_party_executor.dispatch` + `normalize_response`，异常封进 `SSHExecResult` 不向上抛；前端 `TaskSchedulerManager.vue` 加 `isThirdPartySwitchParamDefinition` 识别 + 新模板分支 + `setScriptParamValue` helper，第三方失败按 crit **不**降级到本地 paramiko；3 个新测试文件 / 16 个新用例全绿。详见 [memory/devops-sandbox.md] 第 13 段。


- **2026-08-05**：第三方端点兜底增强——首版兜底仅覆盖 `endpoints_json` 空串，实测用户环境 `loaded_endpoints=[]` 且无解析 warning，判定污染值为空数组 `[]`（json 解析成功 0 端点），首版兜底不触发。`ThirdPartyEndpointRegistry.load_from_settings` 重构：抽离 `_load_raw()` 解析器，全局懒加载路径（未注入 settings）在**解析后 0 端点**时从项目根 `.env` 文件读取原始值**重载**（覆盖空串 / `[]` / 非法 JSON / 无 primary 等全部污染形态）。新增 `test_registry_global_lazy_load_fallback_when_env_overridden_to_empty_array`；50 个相关测试全绿。
- **2026-08-05**：修复第三方端点 `primary` 未配置第三层根因——**os.environ 空值污染**：pydantic-settings 环境变量优先级高于 `.env` 文件，运行环境（IDE 调试配置 / shell profile）存在空值 `THIRD_PARTY_EXECUTOR_ENDPOINTS` 时覆盖 `.env` 配置（实测 endpoints_json 变 0，allow_insecure 仍 True——解释"其他参数正常"）。`ThirdPartyEndpointRegistry.load_from_settings` 全局懒加载路径在 `endpoints_json` 为空时通过 `_read_env_file_endpoints_fallback()` 从项目根 `.env` 文件兜底读取。新增 `test_registry_global_lazy_load_falls_back_to_env_file` / `test_env_override_empty_empties_new_settings_instance` 2 用例；49 个相关测试全绿。
- **2026-08-05**：修复第三方端点 `primary` 未配置第二层根因——settings.py 全部 `env_file` 由相对路径 `".env"` 改为绝对路径 `_ENV_FILE_PATH`（基于 `Path(__file__).resolve().parents[3]` 推导，不依赖进程 CWD）。原相对路径在 CWD 非项目根（IDE 调试 / 定时任务 / 运维脚本入口）时整个 `.env` 加载失败，表现为"配置了却报 primary 未配置"；实测 CWD=用户目录时 endpoints_json 从 632 字符变 0、allow_insecure 变 False。新增 `test_env_file_path_is_absolute_and_exists` / `test_third_party_settings_cwd_independent` 2 用例（后者 monkeypatch.chdir 验证 CWD 无关性）。SSHTools 第三方失败分支新增 logger.error 控制台诊断（error_code / endpoint / exc / loaded_endpoints）。
- **2026-08-05**：修复第三方端点 `primary` 未配置——`ThirdPartyExecutorSettings.endpoints_json` 字段默认 env 名是 `THIRD_PARTY_EXECUTOR_ENDPOINTS_JSON`（env_prefix + 字段名），与 `.env` 契约 `THIRD_PARTY_EXECUTOR_ENDPOINTS` 不一致导致配置读不到；改为 `validation_alias=AliasChoices("endpoints_json", "THIRD_PARTY_EXECUTOR_ENDPOINTS")`，新增 `app/tests/core/config/test_third_party_settings.py` 3 用例，`test_third_party_executor.py` 27 用例全绿。
- **2026-08-05**：SSHTools 三个 `@tool` 改 async def + asyncio.to_thread 包装 paramiko 同步阻塞点；第三方分支直接 `await dispatch(...)` 消除 `asyncio.run() / run_coroutine_threadsafe(...)` 在 LangGraph ToolNode in-flight loop 内的 RuntimeError / 死锁；`_emit_log` / `_emit_batch_failure_with_members` 同步改 async（顺带修潜在日志丢失 bug）；新增 3 个 in-flight loop 回归测试；devops 目录 64 → 123 全绿。
- **2026-08-05**：第三方分支失败日志可观测性补强——`ThirdPartyEndpointRegistry.diagnostic_summary()` 公开 `[name, enabled, url]` 摘要 API，SSHTools 第三方路径（成功 + 失败）写入 `metadata.loaded_endpoints` + `loaded_endpoint_count`。运维看日志即可区分「name 拼错」「JSON/PEM/URL 配错（loaded_endpoints=[]）」「enabled=False（loaded_endpoints=[{enabled: False}]）」三类根因；摘要严格不含 `public_key_pem`。新增 6 个回归用例；devops 目录 123 → 127 全绿。
- **2026-07-30**：审计日志新增可信 IP 字段（`AgentContext.log_ip` + `request.client.host` 强制覆盖 + `SSHTools._emit_log` 写入 `LogEvent.ip_address`），SSH 工具审计日志补全客户端 IP（修复前 6 行 `ssh_execute_command` 全部 NULL）。
- **2026-07-29**：统一日志 `LogService` + 迁移 auth/user/session/SSH + admin 查询 API + 可信身份双层 + 命令/凭据脱敏 + 队列预留 + 197 测试 GREEN。
